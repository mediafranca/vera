-- allium: 3
-- offline-reconciliation.allium

-- Scope: The local-first path between a participant's gesture and Vera's canonical log
-- Includes: Immediate local application, durable pending work, observable synchronisation,
--   ordered push and pull, restart recovery, incremental projection and explicit conflicts
-- Excludes:
--   - The browser database, worker, transport and rendering libraries
--   - The meaning and canonical application of changes (see change-application.allium)
--   - Authentication and authorization policy (see identity-access.allium)
--   - Automatic semantic merging of divergent prose
--
-- North star: the hand never waits for the network. Reading, writing, splitting,
-- moving and navigating are local acts. Connectivity changes how soon other devices
-- learn about them, never how soon this device lets the participant continue thinking.

use "./core.allium" as core
use "./change-application.allium" as application

------------------------------------------------------------
-- Contracts
------------------------------------------------------------

contract MaximallyFluidLocalFirstWorkspace {
    accept_locally: (replica: LocalReplica, change: core/Change) -> PendingChange
    synchronise: (replica: LocalReplica) -> SynchronisationState
    resolve: (conflict: Conflict, resolution: ConflictResolution) -> PendingChange

    @invariant TheHandNeverWaitsForTheNetwork
        -- An available local replica applies an accepted gesture and presents its
        -- consequence without waiting for a request, acknowledgement or server render.
        -- Network latency, loss and server unavailability cannot delay the next gesture.

    @invariant LocalDurabilityPrecedesSavedFeedback
        -- Vera says that a local change is saved only after the change and its origin
        -- identity can survive closing and reopening the application on this device.

    @invariant TheCanonicalLogRemainsCanonical
        -- Local immediacy does not create a second authority. The server's ordered,
        -- idempotent operation log remains the shared history; the replica converges
        -- toward it and makes every departure from it observable.

    @invariant SynchronisationIsBackgroundWork
        -- Push, pull, retry and projection continue independently of reading and
        -- editing. No synchronisation cycle locks the page, caret or navigation.

    @invariant RenderingFollowsChangedMeaning
        -- A text edit updates text immediately. Derived maps, links, tags, queries and
        -- Markdown are recomputed only when their inputs changed, and an acknowledgement
        -- that changes no meaning does not redraw the whole page.

    @invariant SilenceNeverPretendsToBeSuccess
        -- The participant can distinguish local, synchronising, synchronised, offline,
        -- rejected and conflicted work without inspecting logs or guessing from delay.
}

------------------------------------------------------------
-- Enumerations
------------------------------------------------------------

enum ReplicaAvailability { empty | hydrating | available }
enum ContentOrigin { canonical | retained }
enum RetainedValidation { unchecked | checking | current | divergent | unreachable }
enum ConnectionState { online | offline }
enum PendingStatus { local | sending | acknowledged | rejected | conflicted }
enum SynchronisationStatus {
    local | synchronising | synchronised | offline | updates_waiting | attention_required
}
enum ConflictResolution { keep_local | keep_canonical | replace_with_participant_edit }

------------------------------------------------------------
-- Entities
------------------------------------------------------------

entity LocalReplica {
    graph: core/Graph
    participant: core/Participant
    availability: ReplicaAvailability
    connection: ConnectionState
    canonical_cursor: Integer
    -- Whether what is on screen came from the server or from what this device had
    -- already kept. It is exposed and not inferred: a participant reading offline
    -- must be able to tell, and «it looks the same» is the reason they cannot.
    showing: ContentOrigin
    pending_changes: PendingChange with replica = this
    conflicts: Conflict with replica = this
    retained_pages: RetainedPage with replica = this

    -- Canonical work that exists past the cursor and has not been taken.
    --
    -- It is counted and announced; it is not applied. Another hand writes into
    -- this corpus — see agent-participation.allium — so a page being read can
    -- have moved underneath while it is being read. Replacing the text under
    -- someone's eyes is not synchronisation, it is interruption; leaving it
    -- unsaid is worse. This number is what makes the third option possible: to
    -- say so, and to let the participant choose when.
    waiting: WaitingWork with replica = this
}

-- A canonical operation this device knows about and has not taken.
--
-- Knowing about it is cheap and taking it is not, and the difference is the whole
-- point. Asking what has happened since the cursor costs a few kilobytes; fetching
-- a page again costs the page. Measured on a real corpus: the last twenty
-- operations, 3.8 KB; one heavily written page, 512 KB. The same answer, a hundred
-- and thirty times lighter.
entity WaitingWork {
    replica: LocalReplica
    operation: application/Operation
    -- Whether it touches what is on screen. What moved elsewhere in the corpus
    -- may wait quietly; what moved here is what the participant is looking at.
    touches_open_page: Boolean
}

entity PendingChange {
    replica: LocalReplica
    origin_id: String
    change: core/Change
    channel: core/ContributionChannel
    evidence: core/OriginEvidence?
    status: PendingStatus
    canonical_sequence: Integer?
    rejection_reason: String?
}

-- Two hands wrote the same block and neither is wrong.
--
-- The block is the unit, and that is not a rendering choice: it is the only thing
-- Vera has an identity for. A line has no name, no author and no history, so a
-- resolution chosen line by line would leave a block whose text neither hand wrote
-- and whose authorship cannot be stated. See core.allium, the stable identity of a
-- block, and agent-participation.allium: in a corpus where a machine also writes,
-- «who said this» has to survive the resolving of a disagreement.
--
-- The lines are shown, because choosing between two versions without seeing what
-- differs is choosing blind. Shown, not chosen between.
entity Conflict {
    replica: LocalReplica
    pending: PendingChange
    canonical_operation: application/Operation
    -- The block both hands touched. It is what the dialogue is about, and what
    -- either resolution ends up holding.
    block: core/Block
    resolved: Boolean
}

entity RetainedPage {
    replica: LocalReplica
    page: core/Page
    -- The delivered content, held on this device so that reading it again does
    -- not require the server.
    content: core/Page
    last_read_at: Timestamp
    validation: RetainedValidation
}

entity SynchronisationState {
    replica: LocalReplica
    status: SynchronisationStatus
    pending_count: Integer
    conflict_count: Integer
    -- What is waiting to be taken, and how much of it is on the open page. The
    -- second number is what decides whether the indicator merely counts or asks.
    waiting_count: Integer
    waiting_here_count: Integer
}

------------------------------------------------------------
-- Phase 1 — Immediate response and truthful feedback
------------------------------------------------------------

rule AcceptChangeIntoAvailableReplica {
    when: ApplyLocally(replica, origin_id, change, channel, evidence?)

    requires: replica.availability = available
    requires: core/Membership{graph: replica.graph, participant: replica.participant}.status = active
    requires: not exists PendingChange{replica: replica, origin_id: origin_id}

    ensures:
        let pending = PendingChange.created(
            replica: replica,
            origin_id: origin_id,
            change: change,
            channel: channel,
            evidence: evidence,
            status: local,
            canonical_sequence: null,
            rejection_reason: null
        )
        LocalProjectionApplied(replica: replica, pending: pending)
        LocalChangeBecameDurable(pending: pending)

    @guidance
        -- The event is complete before any submission begins. Structural gestures such
        -- as splitting, moving and creating blocks follow this same path; they are not
        -- decomposed into a chain of blocking server round trips.
}

rule PresentSynchronisationState {
    when: SynchronisationStateRequested(replica)

    requires: replica.availability = available

    ensures: SynchronisationState.created(
        replica: replica,
        status: visible_status(replica),
        pending_count: count(replica.pending_changes where status != acknowledged),
        conflict_count: count(replica.conflicts where resolved = false)
    )
}

------------------------------------------------------------
-- Phase 2 — Durable outbox and useful local memory
------------------------------------------------------------

rule RestoreDurableLocalWork {
    when: ReplicaOpened(replica)

    ensures:
        replica.availability = hydrating
        DurableReplicaLoaded(replica: replica)
        replica.availability = available

    @guidance
        -- Opening first restores locally held content, cursor and pending changes.
        -- Contact with the server may begin afterwards and never gates availability.
}

rule ReturnPendingChangeAfterRestart {
    when: DurableReplicaLoaded(replica)

    ensures:
        for pending in durable_pending_changes(replica):
            pending.status = local

    @guidance
        -- A change interrupted while sending is safe to retry because origin_id is
        -- idempotent. Restarting never turns uncertainty into loss or duplication.
}

rule RetainDeliveredPage {
    when: PageDelivered(replica, page, origin)

    requires: origin = canonical

    ensures:
        RetainedPage{replica: replica, page: page}.content = page
        RetainedPage{replica: replica, page: page}.last_read_at = now()
        replica.showing = canonical
        BoundedRetentionEnforced(replica: replica)

    @guidance
        -- What is retained is what was read. Vera does not replicate the corpus onto
        -- a device in advance: a graph of thousands of pages does not fit on a phone,
        -- and choosing a subset in advance means guessing at attention. Reading is
        -- that guess already made, by the person, out loud — and it is self-correcting,
        -- because what someone returns to is what they return to.
}

rule ForgetTheLeastRecentlyRead {
    when: BoundedRetentionEnforced(replica)

    requires: count(RetainedPages{replica: replica}) > retention_limit(replica)

    ensures:
        for page in least_recently_read(replica, beyond: retention_limit(replica)):
            not exists RetainedPage{replica: replica, page: page}

    @guidance
        -- Retention is bounded because device storage is, and it is dropped by last
        -- read and not by size: the largest page is not the least wanted. Dropping
        -- retained content is never a loss — it is canonical elsewhere — which is
        -- what makes this safe and what distinguishes it from the outbox, where
        -- dropping anything would be.
}

rule ShowRetainedPageAtOnce {
    when: PageRequested(replica, page)

    requires: exists RetainedPage{replica: replica, page: page}

    ensures:
        replica.availability = available
        PageDelivered(
            replica: replica,
            page: RetainedPage{replica: replica, page: page}.content,
            origin: retained
        )
        replica.showing = retained
        RetainedPage{replica: replica, page: page}.validation = checking
        CanonicalValidationRequested(replica: replica, page: page)
        BackgroundSynchronisationBegins(replica: replica)

    @guidance
        -- A page this device has been given before is shown from this device, at once,
        -- whether or not the server would have answered. That is what
        -- @guarantee LocalFirstMeansFirst has said all along, and the rule that used to
        -- stand here said the opposite: it required the delivery to have failed first,
        -- so a reachable-but-slow server was waited for with the page already in hand.
        --
        -- The cost of that was not theoretical. Opening one heavily written page of a
        -- real corpus is 0.81 s and 512 KB, paid again on every visit, because nothing
        -- asks whether the copy already held is still good. A page in the breadcrumb,
        -- opened for the fiftieth time, paid it fifty times. Over a tunnelled link that
        -- is the difference between a workspace and a form.
        --
        -- The operation cursor remains the cheap continuous question. Opening a
        -- retained page additionally validates that exact snapshot once: a cursor
        -- may itself have been retained too early, and equal block counts do not make
        -- equal prose. Until that answer arrives the page says that it is checking.
        --
        -- The replica seeded from retained content accepts gestures exactly as one
        -- seeded from a canonical delivery, so a page read yesterday can be written into
        -- today with no connection at all, and what is written leaves through the outbox
        -- like everything else.
}

rule ConfirmRetainedPageIsCurrent {
    when: CanonicalValidationAnswered(replica, page, canonical)

    let retained = RetainedPage{replica: replica, page: page}

    requires: exists retained
    requires: same_page_meaning(retained.content, canonical)

    ensures:
        retained.validation = current
        replica.showing = canonical
        RetainedSnapshotConfirmed(replica: replica, page: page)
}

rule ReplaceDivergentRetainedPageBeforeWriting {
    when: CanonicalValidationAnswered(replica, page, canonical)

    let retained = RetainedPage{replica: replica, page: page}

    requires: exists retained
    requires: not same_page_meaning(retained.content, canonical)
    requires: not participant_is_editing(page)
    requires: not exists pending_change_for_page(replica, page)

    ensures:
        retained.validation = divergent
        PageDelivered(replica: replica, page: canonical, origin: canonical)
        RetainedDivergenceAnnounced(replica: replica, page: page)

    @guidance
        -- This is cache validation at the threshold of a reading, not remote work
        -- arriving underneath an established reading. A stale cache has no authored
        -- claim of its own, so replacing it is correction rather than conflict.
}

rule PreserveWritingWhenRetainedPageDiverged {
    when: CanonicalValidationAnswered(replica, page, canonical)

    let retained = RetainedPage{replica: replica, page: page}

    requires: exists retained
    requires: not same_page_meaning(retained.content, canonical)
    requires: participant_is_editing(page) or exists pending_change_for_page(replica, page)

    ensures:
        retained.validation = divergent
        RetainedDivergenceRequiresAttention(replica: replica, page: page)
        no PageDelivered(replica: replica, page: canonical, origin: canonical)

    @guidance
        -- Validation never erases text under a hand. The local draft remains visible
        -- and recoverable while Vera says plainly that the corpus has another version.
}

rule SayWhenRetainedPageCouldNotBeValidated {
    when: CanonicalValidationFailed(replica, page)

    let retained = RetainedPage{replica: replica, page: page}

    requires: exists retained

    ensures:
        retained.validation = unreachable
        RetainedValidationFailureAnnounced(replica: replica, page: page)
}

rule DeliverCanonicallyWhatWasNeverRead {
    when: PageRequested(replica, page)

    requires: not exists RetainedPage{replica: replica, page: page}

    ensures: CanonicalDeliveryRequested(replica: replica, page: page)

    @guidance
        -- A page this device has never held has to come from somewhere, and there is
        -- only one somewhere. This is the ordinary first read, and it is the only one
        -- that waits.
}

rule OpenWorkspaceWithoutTheServer {
    when: ReplicaOpened(replica)

    ensures:
        DurableReplicaLoaded(replica: replica)
        replica.availability = available
        BackgroundSynchronisationBegins(replica: replica)

    @guidance
        -- Opening cannot depend on an answer from the network. A workspace that shows
        -- "could not reach the server" and nothing else has made connectivity a
        -- condition of reading one's own memory, which is the one thing a sovereign
        -- memory may not do. See workspace-interface.allium, invariant
        -- OfflineReadingAndWriting.
        --
        -- This is equally binding when the server is reachable but slow. Retained
        -- corpus state, page index and readable page content open the workspace first;
        -- checking health, refreshing the index and enriching derived relations happen
        -- behind that reading. Connectivity is not local-first merely because it will
        -- eventually time out.
}

------------------------------------------------------------
-- Phase 3 — Replica, cursor and automatic reconciliation
------------------------------------------------------------

rule SubmitNextLocalChange {
    when: BackgroundSynchronisationBegins(replica)

    let pending = first_sendable_pending_change(replica)

    requires: replica.availability = available
    requires: replica.connection = online
    requires: exists pending
    requires: not exists unresolved_conflict_for(pending)

    ensures:
        pending.status = sending
        core/ParticipantSubmits(
            participant: replica.participant,
            graph: replica.graph,
            origin_id: pending.origin_id,
            change: pending.change,
            channel: pending.channel,
            evidence: pending.evidence
        )

    @guidance
        -- Pending changes leave the device in their local order. Repeated edits may be
        -- compacted before this point only when the observable final graph, provenance
        -- and undoable participant intent remain identical.
}

rule AcknowledgeCanonicalOperation {
    when: operation: application/Operation.created

    let pending = PendingChange{origin_id: operation.origin_id}

    requires: exists pending
    requires: pending.replica.graph = operation.log.graph

    ensures:
        pending.status = acknowledged
        pending.canonical_sequence = operation.sequence
        pending.replica.canonical_cursor = max(pending.replica.canonical_cursor, operation.sequence)
        CanonicalAcknowledgementAppliedWithoutFullReload(replica: pending.replica, operation: operation)
}

rule PullOperationsAfterCursor {
    when: BackgroundSynchronisationBegins(replica)

    requires: replica.availability = available
    requires: replica.connection = online

    ensures:
        for operation in canonical_operations(replica.graph, after: replica.canonical_cursor):
            WaitingWork.created(
                replica: replica,
                operation: operation,
                touches_open_page: operation_touches(operation, replica)
            )
        CanonicalWorkWaits(replica: replica)

    @guidance
        -- The cheap question, and the only one asked on a timer: what has happened
        -- since my cursor. It is answered with the operations themselves, which are
        -- small — a few kilobytes for a day's work — and not with the pages they
        -- touched, which are not.
        --
        -- Nothing is applied here. The operations are known about; taking them is a
        -- separate act with a separate rule, because it is a decision and not a
        -- consequence.
}

rule AnnounceWaitingCanonicalWork {
    when: CanonicalWorkWaits(replica)

    requires: count(replica.waiting) > 0

    ensures: WaitingWorkIsVisible(replica: replica, here: count(replica.waiting where touches_open_page))

    @guidance
        -- The indicator changes and waits to be pressed. It does not count down, it
        -- does not fade, and it does not eventually give up and apply anyway: a thing
        -- that acts on its own after being ignored has not asked, it has warned.
        --
        -- Two states and not one, because they are two different pieces of news.
        -- Something moved in the corpus is worth knowing; something moved in the page
        -- on screen is worth deciding about now.
}

rule TakeWaitingCanonicalWork {
    when: ParticipantTakesWaitingWork(participant, replica)

    requires: replica.participant = participant
    requires: replica.connection = online

    ensures:
        for waiting in replica.waiting:
            RemoteOperationReceived(replica: replica, operation: waiting.operation)

    @guidance
        -- Taking is the participant's act, and it is the only thing that moves canonical
        -- work into what is on screen. Everything downstream — applying what is
        -- independent, exposing what collides — hangs from here.
}

rule ApplyConflictFreeRemoteOperation {
    when: RemoteOperationReceived(replica, operation)

    requires: not conflicts_with_pending_change(replica, operation)

    ensures:
        CanonicalOperationAppliedLocally(replica: replica, operation: operation)
        replica.canonical_cursor = operation.sequence
        not exists WaitingWork{replica: replica, operation: operation}
        IncrementalProjectionUpdated(replica: replica, operation: operation)

    @guidance
        -- Once taken, what does not collide simply applies: the participant said yes to
        -- the news, and asking again per operation would turn one decision into fifty.
        -- The dialogue below is for the ones that cannot be applied without choosing.
}

rule ContinueWhenConnectionReturns {
    when: ConnectionRestored(replica)

    ensures:
        replica.connection = online
        BackgroundSynchronisationBegins(replica: replica)
}

------------------------------------------------------------
-- Phase 4 — Exceptional divergence without silent loss
------------------------------------------------------------

rule ExposeConcurrentConflict {
    when: RemoteOperationReceived(replica, operation)

    let pending = conflicting_pending_change(replica, operation)

    requires: exists pending

    ensures:
        pending.status = conflicted
        Conflict.created(
            replica: replica,
            pending: pending,
            canonical_operation: operation,
            block: contested_block(pending, operation),
            resolved: false
        )
        ConflictRequiresParticipantAttention(replica: replica, pending: pending)

    @guidance
        -- Vera may automatically reconcile independent subjects and idempotent repeats.
        -- It never silently chooses between two incompatible versions of the same prose.
        --
        -- This is reached only through taking, so a conflict is never news that arrives
        -- on its own: it is what the participant found when they went to look.
}

rule ResolveConcurrentConflict {
    when: ParticipantResolvesConflict(participant, conflict, resolution, replacement?)

    requires: conflict.replica.participant = participant
    requires: conflict.resolved = false
    requires: resolution != replace_with_participant_edit or replacement != null

    ensures:
        conflict.resolved = true
        let pending = resolved_pending_change(conflict, resolution, replacement?)
        pending.status = local
}

rule PreserveRejectedLocalChange {
    when: SubmissionRejected(replica, origin_id, reason)

    let pending = PendingChange{replica: replica, origin_id: origin_id}

    requires: exists pending

    ensures:
        pending.status = rejected
        pending.rejection_reason = reason
        RejectedChangeRemainsRecoverable(pending: pending)
}

------------------------------------------------------------
-- Invariants
------------------------------------------------------------

invariant PendingOriginIsUniqueWithinReplica {
    for a in PendingChanges:
        for b in PendingChanges:
            a != b implies a.replica != b.replica or a.origin_id != b.origin_id
}

invariant AcknowledgementNamesCanonicalPosition {
    for pending in PendingChanges:
        pending.status = acknowledged implies pending.canonical_sequence != null
}

invariant UnresolvedConflictIsNeverReportedAsSynchronised {
    for state in SynchronisationStates:
        state.conflict_count > 0 implies state.status = attention_required
}

invariant OfflineWorkIsNotReportedAsCanonical {
    for pending in PendingChanges:
        pending.status = local or pending.status = sending implies pending.canonical_sequence = null
}

invariant RetentionIsBounded {
    for replica in LocalReplicas:
        count(replica.retained_pages) <= retention_limit(replica)
}

invariant RetainedPageBelongsToTheReplicaGraph {
    for retained in RetainedPages:
        retained.page.graph = retained.replica.graph
}

-- Nothing crosses from the corpus into what is on screen except by being taken.
--
-- It is the invariant form of WhatArrivesIsAnnouncedAndNotImposed, and it is worth
-- stating separately because the failure it forbids is silent: an operation that
-- applied itself would look exactly like one the participant took.
invariant NothingArrivesUnasked {
    for work in WaitingWork:
        work.operation.sequence > work.replica.canonical_cursor
}

-- Waiting work is never reported as being up to date. It is the same promise
-- UnresolvedConflictIsNeverReportedAsSynchronised makes about conflicts: an
-- indicator that says «synchronised» while something waits has lied about the one
-- thing it exists to say.
invariant WaitingWorkIsNeverReportedAsSynchronised {
    for state in SynchronisationStates:
        state.waiting_count > 0 implies state.status != synchronised
}

invariant CursorAdvancesOnlyThroughCanonicalOperations {
    for replica in LocalReplicas:
        replica.canonical_cursor >= 0
}

------------------------------------------------------------
-- Actors and surfaces
------------------------------------------------------------

actor LocalParticipant {
    within: LocalReplica
    identified_by: core/Participant where this = within.participant
}

surface LocalFirstWorkspace {
    facing participant: LocalParticipant
    context replica: LocalReplica

    exposes:
        replica.availability
        replica.connection
        replica.canonical_cursor
        replica.showing
        replica.pending_changes
        replica.conflicts
        replica.retained_pages
        replica.waiting

    provides:
        ApplyLocally(replica, origin_id, change, channel, evidence?)
        SynchronisationStateRequested(replica)
        ParticipantTakesWaitingWork(participant, replica)
        ParticipantResolvesConflict(participant, conflict, resolution, replacement?)
        PageRequested(replica, page)
        PageDelivered(replica, page, origin)

    contracts:
        fulfils MaximallyFluidLocalFirstWorkspace

    @guarantee LocalFirstMeansFirst
        -- The local replica is not a cache consulted when the server is slow. It is the
        -- workspace the participant uses. The network reconciles work already done.

    @guarantee WhatWasReadStays
        -- A page that has been read is readable again on that device without a
        -- server, and writable there too. Nothing is replicated in advance: reading
        -- is what selects, which is both the cheapest possible policy and the only
        -- one that follows a person's attention instead of guessing at it.

    @guarantee WhatArrivesIsAnnouncedAndNotImposed
        -- Canonical work that appeared after the cursor changes an indicator and waits
        -- to be pressed. It is never applied to what is on screen on its own.
        --
        -- The reason is that another hand writes into this corpus. A librarian agent
        -- can rewrite a page while it is being read, and there are exactly three things
        -- a workspace can do about that. It can replace the text under the reader's
        -- eyes, which is not synchronisation but interruption. It can say nothing,
        -- which is the one thing @invariant SilenceNeverPretendsToBeSuccess forbids.
        -- Or it can say so and let the owner choose when — which costs a button and is
        -- the only one of the three that treats reading as something in progress.
        --
        -- It follows that a page may be read knowingly out of date, and that is a
        -- feature and not a tolerated defect: the alternative is a text that moves while
        -- it is being read.

    @guarantee KnowingIsCheapAndTakingIsNot
        -- What the indicator costs to keep true is the question «what has happened
        -- since my cursor», not the page. Measured on a real corpus: the last twenty
        -- operations, 3.8 KB and 3 ms; one heavily written page, 512 KB and 0.81 s.
        --
        -- This is why announcing can be continuous and taking has to be asked for. A
        -- design that had to refetch pages to know whether anything changed would have
        -- to choose between being current and being usable.

    @guarantee AUsablePageArrivesBeforeItsDerivations
        -- On a first read, the title, properties, writing and the provenance needed
        -- to distinguish that writing are presented before any information that
        -- requires traversing the rest of the graph. Relations, references and
        -- conceptual projections arrive afterwards without retransmitting or
        -- recomposing the writing already shown.
        --
        -- Superseded deliveries are abandoned when attention moves to another page:
        -- work for the previous page must not compete with the page now being read.

    @guarantee ADisagreementIsResolvedOneBlockAtATime
        -- Where two hands wrote the same block, the participant is shown both versions
        -- with the differing lines marked, and chooses one — or writes a third.
        --
        -- The block and not the line, because the block is the only thing Vera has an
        -- identity for. Merging line by line produces text that neither hand wrote,
        -- attributed to a block whose authorship can then no longer be stated, in a
        -- corpus where knowing which hand wrote what is the point.
        --
        -- The lines are shown because choosing between two versions without seeing
        -- where they differ is choosing blind.

    @guarantee FluencyIsEndToEnd
        -- Fast keystrokes are insufficient if Enter, navigation, Markdown, a map redraw
        -- or a synchronisation acknowledgement can freeze the same hand afterwards.
        -- Every phase is complete only when the whole gesture-to-next-gesture path stays
        -- responsive under latency, packet loss and temporary server absence.
}

------------------------------------------------------------
-- Open Questions
------------------------------------------------------------

open question "What device-level latency budgets will define perceptual immediacy for text, structural gestures, navigation and derived projections?"
-- Answered in part: nothing is replicated initially, so there is no interval before
-- full hydration to be available during. What is retained is what was read, bounded,
-- dropped by last read. What that leaves open is narrower and is below.

open question "Beyond the pages that were read, should anything be retained ahead of being asked for — the day in progress, what an open page links to — and does pulling in neighbours defeat the point of letting attention do the selecting?"

open question "What sets the retention bound on a device whose storage Vera cannot measure, and should a participant be able to say 'keep this page whatever happens' for the ones they need on a plane?"
open question "What happens to pending work when authentication expires while a device is offline?"
open question "Which local data is retained or erased on sign-out, device transfer and participant removal?"
open question "What recovery path preserves pending work when durable local storage is full, unavailable or corrupted?"
open question "Which same-subject changes are provably safe to merge automatically beyond idempotent repeats and changes to independent subjects?"
